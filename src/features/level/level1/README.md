# Уровень 1

Сценарий собран по документу `1_uroven_dop_vopr.docx`: сцена с Машей и Ваней, Луно, игры 1–5 и итоговый тест.

## Редактирование текстов

Основной сценарий и списки данных: **`level1FlowData.js`**.

## Картинки

В исходном `.docx` вложенных изображений не было (только текст). Если добавите иллюстрации — положите файлы в `public/level1/` и укажите пути в данных или в компонентах.

## Прогресс

Шаг сохраняется в `localStorage` под ключом `level_1_<uid>`.
